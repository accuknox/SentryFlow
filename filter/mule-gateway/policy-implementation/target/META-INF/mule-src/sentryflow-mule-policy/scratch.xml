<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:policy="http://www.mulesoft.org/schema/mule/policy"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/policy http://www.mulesoft.org/schema/mule/policy/current/mule-policy.xsd">

  <policy:proxy name="test-policy">
    <policy:after>
      <http:request method="POST"
                    url="#[
                      var host = '{{sentryflowHost}}'
                      ---
                      if (host startsWith 'http://' or host startsWith 'https://')
                        (host ++ '{{sentryflowPath}}')
                      else
                        ('http://' ++ host ++ '{{sentryflowPath}}')
                    ]"
                    responseTimeout="{{timeoutMs}}"
                    sendBodyMode="ALWAYS" />
    </policy:after>
  </policy:proxy>
</mule>
